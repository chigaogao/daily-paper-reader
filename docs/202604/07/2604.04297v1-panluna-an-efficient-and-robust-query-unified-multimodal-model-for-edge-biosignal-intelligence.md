---
title: "PanLUNA: An Efficient and Robust Query-Unified Multimodal Model for Edge Biosignal Intelligence"
title_zh: PanLUNA：一种用于边缘生物信号智能的高效且鲁棒的查询统一多模态模型
authors: "Marija Zelic, Anna Tegon, Yawei Li, Thorir Mar Ingolfsson, Luca Benini"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04297v1"
tags: ["query:eeg-ai"]
score: 9.0
evidence: 在共享编码器中处理EEG的泛模态基础模型
tldr: PanLUNA是一个轻量级（5.4M参数）的全模态生理信号基础模型，通过统一查询机制整合了EEG、ECG和PPG信号。它解决了多模态配对数据稀缺的问题，在保持高性能的同时，对缺失模态具有鲁棒性。该模型在边缘设备（如GAP9微控制器）上表现出极高的能效和低延迟，适用于可穿戴设备的实时生物信号智能分析。
selection_source: fresh_fetch
motivation: 现有的生理基础模型多局限于单一模态且缺乏对缺失模态的鲁棒性，难以在资源受限的边缘设备上高效运行。
method: 扩展了LUNA的通道统一模块，将多模态通道视为带有传感器类型嵌入的统一查询集，实现高效的跨模态早期融合。
result: "在TUAB异常检测和HMC睡眠分期任务中达到或超过了参数量大57倍的模型，且INT8量化后仍保持96%以上的性能。"
conclusion: PanLUNA证明了紧凑型多模态基础模型在边缘侧生物信号处理中的可行性，兼顾了高精度、鲁棒性和极低的功耗。
---

## 摘要
生理基础模型 (FMs) 在生物信号表示学习方面展现出巨大潜力，但由于成对多模态数据集的稀缺，大多数模型仍局限于单一模态（如 EEG、ECG 或 PPG）。在本文中，我们提出了 PanLUNA，这是一个参数量仅为 5.4M 的紧凑型全模态基础模型，能够在单个共享编码器中共同处理 EEG、ECG 和 PPG。通过扩展 LUNA 的通道统一模块，PanLUNA 将多模态通道视为统一查询集中的条目，并辅以传感器类型嵌入，从而实现了高效的跨模态早期融合，同时在推理时对缺失模态具有内在的鲁棒性。尽管其占用空间很小，PanLUNA 的性能仍可媲美或超过比其大 57 倍的模型：在 TUAB 异常 EEG 检测中达到 81.21% 的均衡准确率，在 HMC 多模态睡眠分期中达到 0.7416 的最先进 (SOTA) 均衡准确率。采用 INT8 权重的量化感知训练恢复了 96% 以上的全精度性能。在用于可穿戴设备的 GAP9 超低功耗 RISC-V 微控制器上部署后，10 秒 12 导联 ECG 推理的延迟为 325.6 ms，功耗为 18.8 mJ；对于 30 秒 epoch 的多模态 5 通道睡眠分期，其推理延迟为 1.206 s，功耗为 68.65 mJ。

## Abstract
Physiological foundation models (FMs) have shown promise for biosignal representation learning, yet most remain confined to a single modality such as EEG, ECG, or PPG, largely because paired multimodal datasets are scarce. In this paper, we present PanLUNA, a compact 5.4M-parameter pan-modal FM that jointly processes EEG, ECG, and PPG within a single shared encoder. Extending LUNA's channel-unification module, PanLUNA treats multimodal channels as entries in a unified query set augmented with sensor-type embeddings, enabling efficient cross-modal early fusion while remaining inherently robust to missing modalities at inference time. Despite its small footprint, PanLUNA matches or exceeds models up to 57$\times$ larger: 81.21% balanced accuracy on TUAB abnormal EEG detection and state-of-the-art 0.7416 balanced accuracy on HMC multimodal sleep staging. Quantization-aware training with INT8 weights recovers $\geq$96% of full-precision performance, and deployment on the GAP9 ultra-low-power RISC-V microcontroller for wearables achieves 325.6 ms latency and 18.8 mJ per 10-second, 12-lead ECG inference, and 1.206 s latency at 68.65 mJ for multimodal 5-channel sleep staging over 30-second epochs.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **PanLUNA**，一个专为边缘设备设计的、高效且鲁棒的多模态生理信号基础模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：人体是一个集成的生物系统，生理状态通常跨模态（如EEG、ECG、PPG）表现。然而，现有的生理基础模型（FMs）大多局限于单一模态，主要原因是成对的多模态数据集稀缺。
*   **核心挑战**：
    1.  **架构冗余**：传统多模态模型多采用“后期融合”（每个模态一个编码器），导致模型体积巨大，难以部署在资源受限的边缘设备。
    2.  **模态缺失**：在实际穿戴场景中，传感器可能脱落或失效，模型需具备处理任意模态组合的鲁棒性。
    3.  **能效瓶颈**：现有的多模态FM计算强度高，无法在超低功耗微控制器（MCU）上实现持续监测。
*   **整体含义**：PanLUNA 通过单一共享编码器统一处理多种生物信号，实现了高性能、轻量化与硬件友好性的平衡。

### 2. 方法论
*   **核心思想**：扩展 LUNA 模型的“通道统一模块”（Channel-Unification Module），将不同模态的通道视为统一查询集（Query Set）中的条目。
*   **关键技术细节**：
    *   **输入表示**：引入“传感器类型嵌入”（Sensor-type Embeddings）来区分 EEG、ECG 和 PPG。
    *   **空间编码**：EEG 使用 3D 电极坐标；ECG 使用基于解剖学的导联角度估计；PPG 使用中性坐标。
    *   **通道-模态统一模块**：利用交叉注意力（Cross-Attention）机制，让一组可学习的潜在查询（Latent Queries）与所有模态的通道特征进行交互，实现早期融合。
    *   **时间编码器**：采用带有旋转位置嵌入（RoPE）的 Patch-wise Transformer 捕捉长程时间依赖。
    *   **预训练策略**：在约 40,000 小时的异构无标签数据上进行自监督预训练（掩码信号重建），无需成对数据。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：TUEG、Siena (EEG)；MIMIC-IV、CODE-15% (ECG)；PulseDB (ECG+PPG)。
    *   **下游任务**：TUAB（EEG 异常检测）、PTB-XL（ECG 多任务）、CSN（ECG 诊断）、HMC（多模态睡眠分期）。
*   **Benchmark 与对比方法**：
    *   对比了 BIOT、LaBraM、LUNA (Base/Large/Huge)、PhysioOmni 等模型。
    *   参数量对比：PanLUNA (5.4M) 对比了从 3.2M 到 386M 不等的模型。
*   **评估场景**：涵盖了全微调（FF）、冻结编码器（FE）、LoRA 适配以及缺失模态下的推理表现。

### 4. 资源与算力
*   **算力支持**：论文提到使用了瑞士国家超算中心（CSCS）的 **Alps** 超级计算机（项目 ID: lp12, lp160）以及 CINECA 的 HPC 资源（项目 IsB32）。
*   **具体细节**：文中未明确给出具体的 GPU 型号数量或总训练时长，但强调了在 **GAP9**（基于 RISC-V 的超低功耗 MCU）上的部署性能。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 5 个大型预训练数据集和 7 个下游评估数据集。
    *   进行了系统的量化实验（INT8/INT4/INT2），对比了后量化（PTQ）与量化感知训练（QAT）。
    *   进行了查询层注意力的可视化实验，验证了模型对不同模态的自动识别能力。
*   **充分性评价**：实验设计非常充分且客观。它不仅在标准基准上刷榜，还专门测试了“缺失模态”这一极具挑战性的现实场景，并完成了从算法到硬件部署的全链路验证。

### 6. 主要结论与发现
*   **性能卓越**：5.4M 参数的 PanLUNA 在 TUAB 任务上媲美比其大 57 倍的模型（如 LUNA-Huge），在 HMC 睡眠分期上达到 SOTA（0.7416 均衡准确率）。
*   **模态鲁棒性**：单一编码器设计使其能自然处理任意模态子集，无需修改架构。
*   **量化友好**：INT8 QAT 可恢复 96% 以上的精度；INT2 权重可实现 16 倍压缩，且性能下降在可接受范围内。
*   **边缘可行性**：在 GAP9 MCU 上，10 秒 ECG 推理仅需 325.6 ms 和 18.8 mJ，支持长达 24 天的连续监测。

### 7. 优点
*   **极高能效**：通过单一编码器避免了多分支架构的参数冗余，非常适合可穿戴设备。
*   **架构通用性**：查询统一机制不仅解决了通道数变化问题，还解决了模态融合问题。
*   **端到端验证**：提供了从自监督预训练到量化再到 RISC-V 硬件部署的完整方案。
*   **可解释性**：可视化证明了潜在查询会自动特化以处理特定的生理模态（如 Query 3 专门响应 ECG 的 QRS 波群）。

### 8. 不足与局限
*   **量化敏感性**：在涉及波形形态（Morphology）的任务（如 PTBXL-Form）中，低比特量化（INT4/INT2）会导致性能出现较明显的绝对下降（2-3%）。
*   **模态覆盖**：目前仅涵盖了 EEG、ECG 和 PPG，尚未扩展到肌电（EMG）或运动信号（IMU）等其他常见穿戴模态。
*   **临床验证**：虽然在公开数据集表现优异，但在真实连续监测环境中的临床可靠性仍需进一步验证。

（完）
