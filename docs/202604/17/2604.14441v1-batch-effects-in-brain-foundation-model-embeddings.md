---
title: Batch Effects In Brain Foundation Model Embeddings
title_zh: 大脑基础模型嵌入中的批次效应
authors: "Ye Tao, Bradley T. Baker, Yu Wu, Anand D. Sarwate, Sandeep Panta, Sergey Plis, Vince D. Calhoun"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14441v1"
tags: ["query:eeg-ai"]
score: 9.0
evidence: 评估BrainLM和SwiFT等大脑基础模型
tldr: 本研究系统评估了BrainLM和SwiFT两种神经影像大模型在多中心fMRI数据集上的表现。研究发现，这些模型的嵌入向量包含显著的批次效应（采集差异），其影响往往超过了诊断相关的生物学信息。此外，研究揭示了不同架构对脑活动表征的偏好差异，强调了在生物医学大模型应用中消除批次干扰、提取真实生物信号的重要性。
selection_source: fresh_fetch
motivation: 探究神经影像大模型在处理异构数据时，能否有效捕捉神经生物学特征，还是会受到多中心采集带来的批次效应干扰。
method: 采用综合评估框架，对BrainLM和SwiFT在多站点fMRI数据集生成的嵌入向量进行系统评估，并研究了数据协调化（Harmonization）的影响。
result: 模型嵌入向量中存在显著的批次相关变异，且BrainLM倾向于捕捉局部区域活动，而SwiFT更擅长表征区域间的相互作用。
conclusion: 神经影像大模型亟需解决批次效应问题，以实现从复杂的采集噪声中分离出具有生物学意义的信号。
---

## 摘要
基础模型在大规模、高维生物医学应用中展现出巨大潜力，但其捕捉相关神经生物学特征的能力仍有待深入探索。我们利用一个全面的评估框架，系统地评估了两种神经影像基础模型（BrainLM 和 SwiFT）在多中心 fMRI 数据集上的嵌入表现。结果表明，基础模型的嵌入编码了大量的批次相关变异性，在异质数据集中，这种变异性往往主导了诊断相关信息。我们进一步研究了旨在减少批次效应的协调化（harmonization）处理如何影响这些嵌入。此外，我们发现 BrainLM 倾向于捕捉细粒度的区域活动，而 SwiFT 则倾向于表征区域间的相互作用，这与其各自的模型架构相一致。本研究强调了在基础模型中考虑批次效应的重要性，并为未来从采集相关的变异性中解耦出具有生物学意义的信号提供了研究动力。

## Abstract
Foundation models show strong potential for large-scale, high-dimensional biomedical applications, yet their ability to capture relevant neurobiological characteristics remains underexplored. We systematically evaluate embeddings from two neuroimaging foundation models, BrainLM and SwiFT, across multi-site fMRI datasets using a comprehensive evaluation framework. Our results show that foundation model embeddings encode substantial batch-related variability, often dominating diagnosis-related information across heterogeneous datasets. We further investigate how harmonization, applied to reduce batch effects, influences these embeddings. In addition, we find that BrainLM prefers to capture fine-grained regional activity, whereas SwiFT tends to represent interactions between regions, consistent with their respective model architectures. Our study highlights the importance of accounting for batch effects in foundation models and motivates future work on disentangling biologically meaningful signals from acquisition-related variability.

---

## 论文详细总结（自动生成）

这篇论文对神经影像基础模型（Brain Foundation Models）在处理多中心数据时的表现进行了深入的系统性评估。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着 Transformer 架构在神经影像学（如 fMRI）中的应用，BrainLM 和 SwiFT 等基础模型展现了强大的特征提取能力。然而，神经影像数据通常来自不同站点（Site），存在由扫描仪硬件、采集协议等引起的“批次效应”（Batch Effects）。
*   **核心问题**：这些模型生成的嵌入向量（Embeddings）究竟是捕捉到了真实的神经生物学信号（如疾病诊断），还是仅仅编码了非生物性的采集差异？如果模型利用批次效应作为预测“捷径”，其临床应用价值和数据隐私安全性将面临重大挑战。

### 2. 论文提出的方法论
研究者建立了一个多维度的评估框架，将基础模型视为固定的特征提取器：
*   **特征提取**：
    *   **BrainLM**：基于 ROI（感兴趣区域）的掩码自编码模型，提取 CLS 标记的隐藏状态作为嵌入。
    *   **SwiFT**：基于全脑体素的对比学习模型，通过对多个时间窗口的嵌入取平均获得受试者级表征。
*   **分析流程**：
    *   **可视化与统计**：利用 PCA 和 LDA 进行降维可视化，并使用 **PERMANOVA**（置换多元方差分析）定量评估站点与诊断对嵌入空间分布的贡献。
    *   **预测性评估**：训练分类器（LDA、逻辑回归、RBF-SVM）分别预测站点身份和临床诊断。
    *   **受控实验设计**：构建“站点-诊断”混淆场景（例如 A 站点全是病人，B 站点全是对照），测试模型是否通过识别站点来伪造高准确率。
    *   **协调化处理**：应用 **ComBat** 算法对嵌入进行后处理，尝试消除站点效应。
    *   **生物学解码**：利用岭回归从嵌入中重建 ALFF（局部活动指标）和 FNC（网络连接指标），分析模型对不同生物信号的偏好。

### 3. 实验设计
*   **数据集**：使用了三个主流的多中心静息态 fMRI 基准数据集：
    1.  **FBIRN**：精神分裂症研究。
    2.  **ADHD-200**：多动症研究。
    3.  **ABIDE I**：自闭症谱系障碍研究。
*   **基准对比（Benchmark）**：将基础模型嵌入与传统的**手工特征 FNC**（功能网络连接性）进行对比。
*   **对比方法**：对比了 BrainLM（侧重局部重建）与 SwiFT（侧重全局对比）两种不同架构的模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究主要将预训练好的基础模型作为特征提取器进行推理和下游统计分析，其核心计算压力在于特征提取和大规模统计建模，而非从头训练大模型。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 3 个大型数据集，涉及数十个采集站点和数千名受试者。
*   **充分性与客观性**：
    *   实验设计非常全面，不仅有定性的可视化，还有定量的统计检验。
    *   通过“受控混淆实验”有力地证明了模型容易利用站点效应作为预测捷径。
    *   对比了不同架构的模型，并引入了传统特征作为对照，实验逻辑严密、客观公平。

### 6. 论文的主要结论与发现
*   **批次效应主导**：基础模型的嵌入中编码了极强的站点信息，其对站点预测的准确率远高于对疾病诊断的预测。在某些数据集中，站点效应在嵌入空间中的解释力完全盖过了生物学差异。
*   **架构偏好差异**：
    *   **BrainLM** 更好地捕捉了**细粒度的区域活动**（ALFF 解码表现更好）。
    *   **SwiFT** 更好地表征了**区域间的相互作用**（FNC 解码表现更好）。
*   **协调化局限性**：传统的 ComBat 协调化虽然能降低站点预测率，但并不能显著增强嵌入中的临床相关信号。
*   **隐私风险**：嵌入中包含的站点信息可能泄露敏感的元数据，引发隐私担忧。

### 7. 亮点
*   **系统性揭示风险**：首次系统性地指出神经影像大模型在多中心应用中存在的“捷径学习”风险。
*   **架构与生物学关联**：深入探讨了模型预训练目标（掩码重建 vs. 对比学习）如何影响其对不同神经生物学特征的敏感度。
*   **方法论严谨**：采用了 PERMANOVA 等稳健的统计工具，而非仅仅依赖分类准确率。

### 8. 不足与局限
*   **模型覆盖有限**：仅评估了 BrainLM 和 SwiFT 两种模型，尚未涵盖所有新兴的脑基础模型。
*   **缺乏主动干预**：研究主要侧重于“发现问题”（后置分析），虽然尝试了 ComBat，但未提出在预训练阶段主动消除批次效应的新算法架构。
*   **应用限制**：研究集中在静息态 fMRI，尚未探讨任务态 fMRI 或其他模态（如结构 MRI、EEG）在基础模型中的批次效应。

（完）
