---
title: "Brain-DiT: A Universal Multi-state fMRI Foundation Model with Metadata-Conditioned Pretraining"
title_zh: Brain-DiT：一种具有元数据条件预训练的通用多状态 fMRI 基础模型
authors: "Junfeng Xia, Wenhao Ye, Xuanye Pan, Xinke Shen, Mo Wang, Quanying Liu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12683v1"
tags: ["query:eeg-ai"]
score: 8.0
evidence: 基于大规模数据集和扩散预训练的通用大脑基础模型
tldr: 针对现有fMRI基础模型在脑状态多样性和预训练任务上的局限，本文提出Brain-DiT。该模型基于24个数据集的近35万条多状态fMRI数据，采用元数据条件的扩散Transformer（DiT）进行预训练。Brain-DiT能学习多尺度表征，在7项下游任务中表现优异，证明了扩散生成式预训练在捕捉神经动力学和全局语义方面的优越性。
selection_source: fresh_fetch
motivation: 现有fMRI基础模型受限于单一脑状态和不匹配的预训练任务，难以学习跨多样化脑状态的通用表征。
method: 提出一种基于元数据条件的扩散Transformer（DiT）预训练框架，利用大规模多状态fMRI数据学习多尺度特征。
result: 实验表明扩散预训练优于传统的重建或对齐任务，且元数据条件能有效分离内在神经动力学与群体变异性。
conclusion: Brain-DiT作为通用的多状态fMRI基础模型，通过生成式预训练显著提升了不同尺度下游任务的性能。
---

## 摘要
当前的 fMRI 基础模型主要依赖于有限的脑状态范围和不匹配的预训练任务，限制了它们在不同脑状态下学习通用表征的能力。我们提出了 Brain-DiT，这是一个通用的多状态 fMRI 基础模型，在来自 24 个数据集的 349,898 个会话（sessions）上进行了预训练，涵盖了静息态、任务态、自然态、疾病态和睡眠态。与以往依赖于原始信号空间或潜空间掩码重构（masked reconstruction）的 fMRI 基础模型不同，Brain-DiT 采用了基于扩散 Transformer (DiT) 的元数据条件扩散预训练，使模型能够学习到同时捕捉细粒度功能结构和全局语义的多尺度表征。通过对 7 个下游任务的广泛评估和消融实验，我们一致发现，基于扩散的生成式预训练是比重构或对齐更强大的代理任务，而元数据条件预训练通过解耦内在神经动力学与群体水平的变异性，进一步提升了下游性能。我们还观察到，下游任务对表征尺度表现出明显的偏好：ADNI 分类更多地受益于全局语义表征，而年龄/性别预测则相对更依赖于细粒度的局部结构。Brain-DiT 的代码和参数可在 https://github.com/REDMAO4869/Brain-DiT 获取。

## Abstract
Current fMRI foundation models primarily rely on a limited range of brain states and mismatched pretraining tasks, restricting their ability to learn generalized representations across diverse brain states. We present \textit{Brain-DiT}, a universal multi-state fMRI foundation model pretrained on 349,898 sessions from 24 datasets spanning resting, task, naturalistic, disease, and sleep states. Unlike prior fMRI foundation models that rely on masked reconstruction in the raw-signal space or a latent space, \textit{Brain-DiT} adopts metadata-conditioned diffusion pretraining with a Diffusion Transformer (DiT), enabling the model to learn multi-scale representations that capture both fine-grained functional structure and global semantics. Across extensive evaluations and ablations on 7 downstream tasks, we find consistent evidence that diffusion-based generative pretraining is a stronger proxy than reconstruction or alignment, with metadata-conditioned pretraining further improving downstream performance by disentangling intrinsic neural dynamics from population-level variability. We also observe that downstream tasks exhibit distinct preferences for representational scale: ADNI classification benefits more from global semantic representations, whereas age/sex prediction comparatively relies more on fine-grained local structure. Code and parameters of Brain-DiT are available at \href{https://github.com/REDMAO4869/Brain-DiT}{Link}.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《Brain-DiT: A Universal Multi-state fMRI Foundation Model with Metadata-Conditioned Pretraining》** 的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 fMRI 基础模型存在三大局限：
    1.  **预训练数据分布受限**：大多仅依赖静息态数据，缺乏对任务态、自然刺激态等多样化脑状态的覆盖。
    2.  **代理任务不匹配**：传统的掩码重构（MAE）易过拟合低信噪比的噪声，而潜空间重构（JEPA）易发生表征坍缩。
    3.  **忽略元数据**：未充分利用临床和人口统计学信息（如年龄、性别、诊断），导致模型难以区分内在神经动力学与群体间的个体差异。
*   **整体含义**：本文提出了 **Brain-DiT**，旨在通过大规模多状态数据和元数据引导的扩散生成式预训练，构建一个能捕捉多尺度特征（从细粒度结构到全局语义）的通用大脑基础模型。

### 2. 论文提出的方法论
*   **核心思想**：将 fMRI 建模为生成式扩散过程，并利用扩散 Transformer（DiT）作为骨干网络，同时引入元数据作为条件信号。
*   **关键技术细节**：
    *   **扩散预训练**：采用 DDPM 前向加噪过程，在原始信号空间学习去噪。使用 **v-prediction** 目标函数（相比预测噪声 $\epsilon$ 或原始信号 $x_0$，在不同信噪比下更稳定）。
    *   **元数据条件注入**：将年龄、性别、疾病诊断等元数据映射为向量，通过 **AdaLN-Zero** 模块注入到 DiT 的每个 Block 中，实现条件化生成。
    *   **多尺度特征聚合**：利用扩散模型天然的层级特性，从不同的去噪时间步（Timesteps）和 Transformer 层（Layers）提取特征。早期时间步捕捉细粒度局部结构，后期时间步编码抽象全局语义。
    *   **查询聚合器（Query-based Aggregator）**：使用可学习的 Query 通过交叉注意力机制融合多尺度特征，为下游任务提供最优表征。

### 3. 实验设计
*   **数据集规模**：使用了 **24 个数据集**，共计 **349,898 个会话（sessions）**。涵盖静息态、任务态、自然视听、疾病（ASD/AD）及睡眠态。
*   **Benchmark 与场景**：
    *   **分布内（ID）任务**：HCP（性别/任务分类）、ABIDE（年龄预测）、SALD（年龄预测）。
    *   **分布外（OOD）任务**：PPMI（帕金森诊断）、ADHD-200（多动症诊断）、NKI（受教育程度/年龄预测）。
*   **对比方法**：包括 BrainLM（掩码语言模型类）、Brain-JEPA（联合嵌入预测类）、BrainMass（对比学习类）以及 SlimBrain（体素级 MAE/JEPA）。

### 4. 资源与算力
*   **硬件环境**：使用了 **6 台 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：预训练进行了 **150 个 epoch**。采用 AdamW 优化器，学习率为 1e-4，使用了混合精度训练。
*   **模型规模**：DiT 骨干网络包含 16 层，隐藏层维度 1024，8 个注意力头。

### 5. 实验数量与充分性
*   **实验组数**：
    *   在 7 个不同的下游数据集上进行了性能评估。
    *   进行了**冻结骨干网络（Frozen Backbone）**的线性评估，验证表征的纯粹质量。
    *   **消融实验**：包括元数据条件的有效性、脑状态多样性的贡献分析、以及不同时间步/层特征的贡献度分析。
*   **客观性与公平性**：实验涵盖了 ID 和 OOD 场景，并针对不同分区方案（AAL424 和 Schaefer-1000）进行了对齐比较，确保了与基准模型对比的公正性。

### 6. 论文的主要结论与发现
*   **性能领先**：Brain-DiT 在所有 7 项下游任务中均显著优于现有最强基准（如 Brain-JEPA 和 BrainMass）。
*   **生成能力**：模型能根据元数据生成高度逼真的 fMRI 信号，生成的群体水平功能连接（FC）矩阵与真实数据高度一致。
*   **状态多样性红利**：实验证明，性能提升主要源于预训练中脑状态的多样性，而非单纯的数据量增加。
*   **尺度偏好性**：发现不同任务对特征尺度有不同偏好。例如，阿尔兹海默症（ADNI）分类更依赖后期时间步的全局语义特征，而年龄预测则需要更细粒度的局部结构信息。

### 7. 优点（亮点）
*   **首创性**：首次将元数据条件扩散模型应用于 fMRI 基础模型预训练。
*   **多状态统一**：打破了以往模型仅限于静息态的僵局，实现了跨状态的通用表征学习。
*   **可解释性**：通过分析聚合器权重，揭示了不同神经科学任务对扩散时间步和网络深度的依赖规律。
*   **解耦能力**：成功利用元数据将个体生理特征与内在神经活动解耦，增强了模型的泛化性。

### 8. 不足与局限
*   **空间分辨率限制**：目前主要基于 ROI（感兴趣区域）的时间序列，而非全脑体素级建模，可能损失了极细微的空间信息。
*   **时间窗口限制**：预训练使用的时间窗口较短（T=40），对于极长程的动态依赖捕捉能力尚待验证。
*   **元数据依赖**：模型性能部分依赖于元数据的质量和完整性，在元数据缺失严重的临床场景下，优势可能会减弱。
*   **计算开销**：扩散模型的迭代推理过程相比单次前向传播的 MAE 类模型，在特征提取阶段计算成本更高。

（完）
