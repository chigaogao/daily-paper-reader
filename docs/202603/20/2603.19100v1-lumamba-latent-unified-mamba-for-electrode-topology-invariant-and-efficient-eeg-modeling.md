---
title: "LuMamba: Latent Unified Mamba for Electrode Topology-Invariant and Efficient EEG Modeling"
title_zh: LuMamba：用于电极拓扑无关且高效脑电建模的潜统一 Mamba
authors: "Danaé Broustail, Anna Tegon, Thorir Mar Ingolfsson, Yawei Li, Luca Benini"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19100v1"
tags: ["query:eeg-ai"]
score: 10.0
evidence: 用于脑电建模和自监督学习的基础模型
tldr: 针对脑电图（EEG）建模中电极拓扑差异和计算效率低下的挑战，本文提出LuMamba框架。该模型结合了LUNA的通道统一机制与Mamba的线性复杂度状态空间建模，并首次在生物信号领域引入LeJEPA预测架构。通过在2.1万小时无标签数据上预训练，LuMamba在多项任务中达到SOTA性能，且计算开销极低，支持超长序列处理。
selection_source: fresh_fetch
motivation: 解决现有EEG模型难以处理不同电极布局且Transformer架构计算复杂度过高的问题。
method: 结合LUNA交叉注意力实现通道统一，利用双向Mamba块进行高效时间建模，并采用掩码重构与LeJEPA联合预训练。
result: 仅凭4.6M参数在阿尔茨海默症检测等任务上达到SOTA，计算量减少377倍，支持序列长度提升12倍。
conclusion: LuMamba证明了结合拓扑不变编码与线性复杂度模型是构建高效、通用EEG基础模型的有效路径。
---

## 摘要
脑电图 (EEG) 能够实现临床和神经技术应用中脑活动的非侵入性监测，但由于电极拓扑结构各异以及计算可扩展性问题（Transformer 架构具有二次序列复杂度），构建 EEG 基础模型仍具挑战。作为联合解决方案，我们提出了 LuMamba (Latent Unified Mamba)，这是一个结合了拓扑无关编码与线性复杂度状态空间建模的自监督框架。该框架利用 LUNA 的学习查询交叉注意力机制进行通道统一，并采用 FEMBA 的双向 Mamba 块进行高效的时间建模。在此架构中，我们首次对用于生物信号学习的潜欧几里得联合嵌入预测架构 (LeJEPA) 进行了系统研究。LuMamba 在来自 TUEG 语料库的超过 21,000 小时无标签 EEG 数据上进行了预训练，并在涵盖异常检测、伪影识别和心理状态分类的五个下游任务上进行了评估，电极配置范围从 16 到 26 个通道。在预训练目标方面，仅使用掩码重构会产生结构化但泛化性较差的表示，而仅使用 LeJEPA 则会产生弥散的嵌入；结合这两个目标可实现最稳健的性能。LuMamba 仅拥有 460 万个参数，在 TUAB 上达到了 80.99% 的均衡准确率，并在阿尔茨海默病检测中实现了最先进的性能 (0.97 AUPR)，同时在等效序列长度下所需的 FLOPS 比最先进模型少 377 倍，且在达到典型 GPU 显存限制前可扩展至 12 倍长的序列。代码可在 https://github.com/pulp-bio/biofoundation 获取。

## Abstract
Electroencephalography (EEG) enables non-invasive monitoring of brain activity across clinical and neurotechnology applications, yet building foundation models for EEG remains challenging due to \emph{differing electrode topologies} and \emph{computational scalability}, as Transformer architectures incur quadratic sequence complexity. As a joint solution, we propose \textbf{LuMamba} (\textbf{L}atent \textbf{U}nified \textbf{Mamba}), a self-supervised framework combining topology-invariant encodings with linear-complexity state-space modeling, using LUNA's learned-query cross-attention mechanism for channel unification~\cite{luna}, and FEMBA's bidirectional Mamba blocks for efficient temporal modeling~\cite{femba}. Within this architecture, we provide the first systematic investigation of the Latent-Euclidean Joint-Embedding Predictive Architecture (LeJEPA) for biosignal learning. Pre-trained on over 21,000 hours of unlabeled EEG from the TUEG corpus, LuMamba is evaluated on five downstream tasks spanning abnormality detection, artifact recognition, and mental condition classification across electrode configurations ranging from 16 to 26 channels. In the pre-training objective, masked reconstruction alone yields structured but less generalizable representations, while LeJEPA alone produces diffuse embeddings; combining both objectives achieves the most robust performance. With only 4.6M parameters, LuMamba attains 80.99\% balanced accuracy on TUAB and achieves state-of-art performance on Alzheimer's detection (0.97 AUPR), while requiring \textbf{377$\times$ fewer FLOPS} than state-of-art models at equivalent sequence lengths and scaling to \textbf{12$\times$ longer sequences} before reaching typical GPU memory limits. Code is available at https://github.com/pulp-bio/biofoundation

---

## 论文详细总结（自动生成）

### LuMamba：用于电极拓扑无关且高效脑电建模的潜统一 Mamba 论文总结

#### 1. 核心问题与研究动机
*   **核心挑战**：
    1.  **电极拓扑异构性**：不同脑电（EEG）数据集的电极数量和位置差异巨大（如 16 到 256 通道），导致模型在跨设备/跨临床场景应用时性能大幅下降。
    2.  **计算复杂度**：主流的 Transformer 架构在处理长序列 EEG 时具有二次方复杂度，难以扩展到长时记录或资源受限的部署环境。
*   **研究动机**：开发一种既能处理任意电极布局，又具备线性计算效率的 EEG 基础模型，并探索比单纯“掩码重构”更有效的自监督预训练目标。

#### 2. 论文提出的方法论
LuMamba 结合了拓扑无关编码与线性复杂度的状态空间模型（SSM），核心组件包括：
*   **通道统一编码（LUNA 机制）**：利用学习到的查询（Learned Queries）进行交叉注意力操作，将不同数量的输入通道投影到固定的潜空间，从而解耦了输入电极配置与模型内部维度。
*   **高效时间建模（FEMBA 机制）**：采用双向 Mamba（bi-Mamba）块替代 Transformer。Mamba 通过选择性状态空间模型实现内容感知的序列处理，且计算复杂度随序列长度线性增长。
*   **LeJEPA 预训练架构**：首次将潜欧几里得联合嵌入预测架构引入生物信号领域。
    *   **SIGReg 正则化**：通过 Epps–Pulley 统计检验，将潜嵌入分布正则化为各向同性的高斯分布。
    *   **JEPA 预测损失**：通过提取信号的局部视图（Local Views）和全局视图（Global Views），训练模型对齐不同尺度的特征表示。
*   **混合目标函数**：结合了“掩码重构损失”和“LeJEPA 损失”，旨在平衡表示的结构化程度与泛化能力。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：TUEG 语料库（约 21,600 小时无标签 EEG，来自 14,000 多名患者）。
    *   **下游任务**：TUAB（异常检测）、TUAR（伪影识别）、TUSL（脑电减慢事件）、APAVA（阿尔茨海默症检测，16通道）、TDBrain（帕金森病检测，26通道）。
*   **Benchmark 与对比方法**：
    *   对比了 BENDR、BIOT、LaBraM、LUNA、EEGFormer 等 SOTA（最先进）基础模型。
    *   在特定疾病检测任务上对比了 Medformer 和 BioMamba。

#### 4. 资源与算力
*   **硬件环境**：使用了 NVIDIA A100-64GB GPU 进行显存和 FLOPS 测试。
*   **算力支持**：研究得到了意大利 CINECA 的 LEONARDO 超级计算机和瑞士国家超算中心（CSCS）Alps 系统的支持。
*   **训练细节**：文中未明确给出总训练时长（小时数），但提到模型参数量仅为 4.6M，且在微调阶段仅训练 30 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同的下游任务，涉及从 16 到 26 通道的异构拓扑。
*   **消融实验**：系统对比了“仅重构”、“仅 LeJEPA”以及“混合目标”三种预训练策略，并通过 t-SNE 可视化分析了潜空间结构。
*   **效率分析**：进行了详尽的计算效率对比，包括不同序列长度下的 FLOPS 消耗和 OOM（显存溢出）极限测试。
*   **评价**：实验设计较为全面，特别是在跨拓扑泛化和长序列扩展性方面的测试非常充分，结果具有客观性。

#### 6. 主要结论与发现
*   **性能表现**：LuMamba 在 TUAB 上达到 80.99% 的均衡准确率；在阿尔茨海默症检测中达到 0.97 AUPR，显著优于现有模型。
*   **效率优势**：在相同序列长度下，LuMamba 的计算量（FLOPS）比 LaBraM 少 **377 倍**，比 LUNA 少 26 倍；支持的序列长度上限比 LUNA 高 **12 倍**。
*   **预训练策略**：单纯的掩码重构虽能产生紧凑的聚类，但跨拓扑泛化差；LeJEPA 能平滑表示几何，提高鲁棒性。两者结合能获得最佳的下游任务性能。

#### 7. 优点
*   **极高的计算效率**：通过 Mamba 架构解决了 EEG 长序列建模的算力瓶颈。
*   **强大的通用性**：LUNA 模块使其能够直接处理任何电极布局，无需重新训练或丢弃通道。
*   **创新的预训练目标**：成功将 LeJEPA 适配至 1D 生物信号，证明了分布正则化对提升泛化性的价值。

#### 8. 不足与局限
*   **类别不平衡任务表现一般**：在高度类别不平衡的 TUSL 任务上，性能略逊于某些特定任务优化的 SOTA 模型。
*   **疾病数据集规模较小**：用于验证泛化性的 APAVA 和 TDBrain 数据集受试者数量较少（分别为 23 和 72 人），可能存在一定的过拟合风险。
*   **预训练成本**：虽然推理和微调高效，但在 2.1 万小时数据上进行大规模预训练仍需极高的算力资源。

（完）
