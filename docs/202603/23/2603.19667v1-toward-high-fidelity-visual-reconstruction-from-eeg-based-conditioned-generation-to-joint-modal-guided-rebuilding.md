---
title: "Toward High-Fidelity Visual Reconstruction: From EEG-Based Conditioned Generation to Joint-Modal Guided Rebuilding"
title_zh: 迈向高保真视觉重建：从基于 EEG 的条件生成到联合模态引导的重构
authors: "Zhijian Gong, Tianren Yao, Wenjia Dong, Xueyuan Xu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19667v1"
tags: ["query:eeg-ai"]
score: 10.0
evidence: 基于EEG的视觉重构与条件生成
tldr: 该研究针对脑电图（EEG）视觉重建中空间和色彩细节丢失的问题，提出了联合模态视觉重建（JMVR）框架。不同于传统的语义对齐方法，JMVR将EEG和文本视为独立模态进行联合学习，并结合多尺度编码策略和图像增强技术。实验证明，该方法在THINGS-EEG数据集上达到了SOTA水平，显著提升了重建图像的保真度、空间结构和色彩还原能力。
selection_source: fresh_fetch
motivation: 现有的EEG视觉重建方法过度依赖语义对齐，导致EEG中丰富的空间和色彩细节在特征压缩过程中丢失。
method: 提出JMVR框架，通过独立处理EEG与文本模态、采用多尺度EEG编码及图像增强技术，保留并利用EEG特有的视觉认知信息。
result: 在THINGS-EEG数据集上的实验表明，JMVR在六个基准测试中均取得领先，尤其在空间结构建模和色彩保真度方面表现卓越。
conclusion: 该研究证明了通过联合模态学习和多尺度特征提取，可以有效实现从脑电信号到高保真视觉图像的精准重建。
---

## 摘要
人类视觉重建旨在根据受试者提供的描述和相应的神经信号重建细粒度的视觉刺激。作为一种被广泛采用的模态，脑电图（EEG）捕捉了丰富的视觉认知信息，涵盖了场景中复杂的空间关系和色彩细节。然而，目前的方法深度耦合于一种对齐框架，该框架强制 EEG 特征与文本或图像的语义表示对齐。这种依赖性可能会压缩 EEG 中丰富的空间和色彩细节，导致仅实现了条件图像生成，而非高保真视觉重建。为了解决这一局限性，我们提出了一种新型的联合模态视觉重建（JMVR）框架。该框架将 EEG 和文本视为独立的模态进行联合学习，以保留用于重建的 EEG 特有信息。它进一步采用了多尺度 EEG 编码策略来捕捉细粒度和粗粒度特征，并结合图像增强来提升感知细节的恢复。在 THINGS-EEG 数据集上的广泛实验表明，JMVR 在与六种基准方法的对比中达到了 SOTA 性能，特别是在建模空间结构和色彩保真度方面表现出卓越的能力。

## Abstract
Human visual reconstruction aims to reconstruct fine-grained visual stimuli based on subject-provided descriptions and corresponding neural signals. As a widely adopted modality, Electroencephalography (EEG) captures rich visual cognition information, encompassing complex spatial relationships and chromatic details within scenes. However, current approaches are deeply coupled with an alignment framework that forces EEG features to align with text or image semantic representation. The dependency may condense the rich spatial and chromatic details in EEG that achieved mere conditioned image generation rather than high-fidelity visual reconstruction. To address this limitation, we propose a novel Joint-Modal Visual Reconstruction (JMVR) framework. It treats EEG and text as independent modalities for joint learning to preserve EEG-specific information for reconstruction. It further employs a multi-scale EEG encoding strategy to capture both fine- and coarse-grained features, alongside image augmentation to enhance the recovery of perceptual details. Extensive experiments on the THINGS-EEG dataset demonstrate that JMVR achieves SOTA performance against six baseline methods, specifically exhibiting superior capabilities in modeling spatial structure and chromatic fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Toward High-Fidelity Visual Reconstruction: From EEG-Based Conditioned Generation to Joint-Modal Guided Rebuilding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于脑电图（EEG）的视觉重建方法大多采用“语义对齐”范式，即强制将 EEG 特征对齐到预训练的文本或图像语义空间。
*   **研究动机**：这种对齐方式虽然降低了训练难度，但会压缩 EEG 中包含的丰富空间结构和色彩细节，导致模型只能生成“语义相关”的图像（条件生成），而无法实现“高保真”的视觉重构。
*   **整体含义**：论文旨在打破对齐限制，通过联合模态学习保留 EEG 特有的感知信息，从而重建出在空间布局和色彩上更接近原始视觉刺激的图像。

### 2. 方法论：核心思想与关键技术
论文提出了 **JMVR（Joint-Modal Visual Reconstruction）** 框架，核心思想是将 EEG 视为与文本平等的独立模态进行联合建模。
*   **多尺度 EEG 编码器**：采用双流结构。左支路通过 1D 卷积和通道注意力捕捉高频时空动态；右支路通过金字塔多尺度池化（PMSP）提取不同时间尺度的全局上下文。
*   **图像增强策略**：在训练阶段，显式提取原始图像的边缘（Canny）、饱和度（HSV）和深度（Depth-Anything-v2）信息，并将其整合进联合潜空间，以强化模型对物理属性的建模。
*   **联合模态注意力（Joint-Modal Attention）**：基于 Diffusion Transformer (DiT) 架构，让图像、文本和 EEG 标记在统一的注意力机制中交互，但各模态拥有独立的投影参数，避免强制对齐。
*   **扩散步长门控（Diffusion Step Gating）**：引入动态权重。在扩散模型去噪的早期（高噪声阶段）侧重文本语义引导，在后期（低噪声阶段）侧重 EEG 提供的细粒度视觉细节。

### 3. 实验设计
*   **数据集**：使用 **THINGS-EEG** 公开数据集，包含 10 名受试者对 16,740 张自然图像的 EEG 响应。
*   **Benchmark 与对比方法**：对比了 6 种 SOTA 方法，包括 NICE (2024)、MUSE (2024)、ATM (2024)、DreamDiffusion (2024)、Perceptogram (2025) 和 CognitionCapturer (2025)。
*   **评价指标**：
    *   像素级：PixCorr, SSIM。
    *   语义级：AlexNet, Inception, CLIP, SwAV。
    *   **创新指标**：引入 **LabEMD**（色彩保真度）和 **DeepEMD**（空间结构一致性），通过计算重建图与原图在色彩空间和深度图上的 Earth Mover's Distance 来评估。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA A40 (48GB) GPU**。
*   **软件/模型**：使用了 Qwen3-VL-Embedding-2B 进行文本描述生成，CLIP-L/G 进行文本编码，以及预训练的 VAE。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了在双 GPU 环境下进行实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 名受试者的完整数据测试，并计算了平均表现。
*   **消融实验**：针对“扩散步长门控”、“多尺度 EEG 编码”和“图像增强”三个核心组件分别做了消融测试，验证了各模块的必要性。
*   **分析深度**：除了定量对比，还进行了 3D 点云重建分析、文本/EEG 遮蔽比例分析、通道注意力空间分布分析以及视觉认知的时序演化分析。
*   **客观性**：实验设计较为全面，通过引入 JMVR*（仅 EEG 输入版本）与现有方法对比，证明了即使不依赖文本，该框架依然具有竞争力，体现了公平性。

### 6. 主要结论与发现
*   **性能领先**：JMVR 在所有评估指标上均达到 SOTA，尤其在 LabEMD 和 DeepEMD 上提升显著，证明了其在色彩和空间重建上的优势。
*   **模态协作**：文本主要负责确定物体类别（语义），而 EEG 负责补充空间布局和色彩细节。
*   **神经机制验证**：引入文本引导后，EEG 的注意力从枕叶（初级视觉区）扩展到了顶叶和颞枕区（高级语义与空间处理区）。
*   **时序特性**：发现语义识别在刺激后 200-300ms 达到峰值，而空间结构的认知呈现“双峰”特性（早期 100-300ms 初步定位，后期 900-1000ms 细节精修）。

### 7. 优点（亮点）
*   **范式创新**：挑战了主流的“对齐”范式，提出了“联合模态”处理 EEG 的新思路。
*   **多尺度建模**：通过 PMSP 模块有效解决了 EEG 信号低信噪比下的特征提取难题。
*   **动态调节**：扩散步长门控机制符合人类视觉处理从粗到精的认知逻辑。
*   **评估全面**：新提出的色彩和深度评估指标填补了该领域对物理属性评价的空白。

### 8. 不足与局限
*   **评估维度**：虽然强化了物理属性评估，但对于更高层级的视觉认知（如物体间的逻辑关系、情感属性等）缺乏深入探讨。
*   **实时性与泛化性**：论文未讨论模型在跨数据集或实时在线重建场景下的表现。
*   **依赖辅助模型**：重建过程依赖于高性能的 VAE、深度估计模型和大型语言模型（用于生成 Caption），这增加了系统的复杂度和推理开销。

（完）
