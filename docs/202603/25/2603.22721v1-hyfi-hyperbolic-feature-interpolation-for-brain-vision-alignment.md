---
title: "HyFI: Hyperbolic Feature Interpolation for Brain-Vision Alignment"
title_zh: HyFI：用于脑-视觉对齐的双曲特征插值
authors: "Sangmin Jo, Wootaek Jeong, Da-Woon Heo, Yoohwan Hwang, Heung-Il Suk"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22721v1"
tags: ["query:eeg-ai"]
score: 8.0
evidence: 将神经活动与语义和感知特征对齐，用于脑-视觉解码
tldr: 本研究针对脑电信号与视觉特征对齐中的模态差异及特征纠缠问题，提出了HyFI框架。该方法利用双曲空间处理信息量差异的优势，通过在双曲测地线上对语义和感知特征进行插值，实现了特征的有效融合与压缩。实验表明，HyFI在THINGS-EEG和THINGS-MEG数据集的零样本脑图检索任务中显著超越了现有技术，大幅提升了检索准确率。
selection_source: fresh_fetch
motivation: 传统方法忽略了脑信号与图像间的信息量差异，且难以处理脑活动中语义与感知特征高度纠缠的特性。
method: 提出HyFI框架，利用双曲空间测地线向原点弯曲的几何特性，在双曲空间内对视觉特征进行插值以实现特征的融合与压缩。
result: "在THINGS-EEG和THINGS-MEG数据集上，该方法将零样本脑图检索的Top-1准确率分别提升了17.3%和9.1%，达到了SOTA性能。"
conclusion: 通过双曲几何建模特征插值能有效缓解脑-视觉对齐中的模态差距和特征纠缠问题，显著提升了脑信号解码的准确性。
---

## 摘要
人工智能的最新进展促使了许多从脑信号中理解和解码人类视觉系统的尝试。以往的研究通常将神经活动与使用预训练视觉模型从图像中提取的语义和感知特征分别进行对齐。然而，这些方法未能解决两个关键挑战：（1）由于脑信号与图像之间表征信息水平的天然差异而产生的模态间隙；（2）语义和感知特征在神经活动中高度纠缠的事实。为了解决这些问题，我们利用了双曲空间，它非常适合考虑信息量的差异，并具有两点之间的测地线自然向原点（表征能力较低处）弯曲的几何特性。利用这些特性，我们提出了一个名为双曲特征插值（HyFI）的新型框架，该框架沿着双曲测地线在语义和感知视觉特征之间进行插值。这实现了感知和语义信息的融合与压缩，有效地反映了脑信号有限的表达能力以及这些特征的纠缠特性。因此，它促进了脑特征与视觉特征之间更好的对齐。我们证明了 HyFI 在零样本脑-图像检索中达到了最先进的性能，在 THINGS-EEG 数据集上 Top-1 准确率提升高达 +17.3%，在 THINGS-MEG 数据集上提升高达 +9.1%，优于以往的方法。

## Abstract
Recent progress in artificial intelligence has encouraged numerous attempts to understand and decode human visual system from brain signals. These prior works typically align neural activity independently with semantic and perceptual features extracted from images using pre-trained vision models. However, they fail to account for two key challenges: (1) the modality gap arising from the natural difference in the information level of representation between brain signals and images, and (2) the fact that semantic and perceptual features are highly entangled within neural activity. To address these issues, we utilize hyperbolic space, which is well-suited for considering differences in the amount of information and has the geometric property that geodesics between two points naturally bend toward the origin, where the representational capacity is lower. Leveraging these properties, we propose a novel framework, Hyperbolic Feature Interpolation (HyFI), which interpolates between semantic and perceptual visual features along hyperbolic geodesics. This enables both the fusion and compression of perceptual and semantic information, effectively reflecting the limited expressiveness of brain signals and the entangled nature of these features. As a result, it facilitates better alignment between brain and visual features. We demonstrate that HyFI achieves state-of-the-art performance in zero-shot brain-to-image retrieval, outperforming prior methods with Top-1 accuracy improvements of up to +17.3% on THINGS-EEG and +9.1% on THINGS-MEG.

---

## 论文详细总结（自动生成）

### 论文总结：HyFI - 用于脑-视觉对齐的双曲特征插值

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是如何有效地将人类脑信号（EEG/MEG）与视觉特征进行对齐，以实现准确的脑解码（如脑图检索）。
*   **研究背景**：现有的脑-视觉对齐方法通常将脑信号分别与图像的“语义特征”（高层类别）和“感知特征”（底层细节）对齐。
*   **核心挑战**：
    1.  **模态间隙（Modality Gap）**：脑信号的信息容量远低于高质量图像，存在严重的信息不对称。
    2.  **特征纠缠（Feature Entanglement）**：在神经活动中，语义和感知信息是高度交织的，而非独立处理，传统双路径独立对齐方法忽略了这种交互性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **HyFI (Hyperbolic Feature Interpolation)** 框架，利用双曲几何（Hyperbolic Geometry）的特性来解决上述挑战。
*   **核心思想**：在具有负曲率的双曲空间（Lorentz模型）中进行特征对齐。双曲空间的测地线（两点间最短路径）会向原点弯曲，而靠近原点的区域表征容量较低，适合建模信息量较少的脑信号。
*   **关键技术细节**：
    1.  **特征提取与增强**：使用预训练视觉模型（如CLIP）提取特征。通过“中央凹模糊（Fovea blur）”增强语义特征 $z_{sv}$，通过“高斯模糊（Gaussian blur）”增强感知特征 $z_{pv}$。
    2.  **双曲插值（Hyperbolic Interpolation）**：在双曲流形上，利用对数映射将特征投影到切空间，进行线性插值后再通过指数映射回流形。公式体现为：$\hat{z}_v = \exp_{z_{sv}}(t \cdot \log_{z_{sv}}(z_{pv}))$。
    3.  **动态系数 $t$**：通过一个可学习的网络根据图像内容动态决定语义与感知特征的融合比例。
    4.  **压缩效应**：论文从数学上证明了双曲插值点比欧几里得插值点更靠近原点，这种“向内收缩”自然实现了特征压缩，使其更符合脑信号的低信息熵特性。
    5.  **对比学习**：在双曲空间内计算脑嵌入 $z_b$ 与插值视觉嵌入 $\hat{z}_v$ 之间的 Lorentz 距离，并进行对比损失训练。

#### 3. 实验设计
*   **数据集**：
    *   **THINGS-EEG**：10名受试者，包含1,654个训练类别和200个测试类别。
    *   **THINGS-MEG**：4名受试者，包含1,854个训练类别和200个测试类别。
*   **任务场景**：200类零样本（Zero-shot）脑图检索（即根据脑信号从200张候选图中检索出对应的图像）。
*   **Benchmark 与对比方法**：对比了 BraVL、NICE、ATM-S、Cog-cap 和 UBP（之前的 SOTA）等多种主流脑解码模型。

#### 4. 资源与算力
*   **硬件环境**：实验在一台 NVIDIA **GTX 1080 Ti (12GB)** GPU 上完成。
*   **训练配置**：使用 AdamW 优化器，学习率 $3 \times 10^{-4}$，Batch Size 为 1024，训练 50 个 epoch。
*   **算力评价**：该方法对算力要求相对较低，单块老旧显卡即可完成训练，具有较好的实用性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了受试者内（Intra-subject）和跨受试者（Inter-subject）两种设定。
    *   进行了详尽的消融实验，验证了双曲空间 vs 欧几里得空间、有无插值机制的效果。
    *   测试了 10 种不同的视觉编码器（ResNet系列、ViT系列、双曲预训练模型如MERU）和 4 种脑信号编码器（EEGNet, TSConv等）。
*   **客观性与公平性**：实验采用了标准化的预处理流程和公开数据集，通过 5 次运行取平均值并进行了显著性检验（$p < 0.01$），结果具有高度的可信度和说服力。

#### 6. 主要结论与发现
*   **性能突破**：HyFI 在两个数据集上均大幅刷新了 SOTA 记录。在 THINGS-EEG 上 Top-1 准确率达到 **68.2%**（提升 17.3%）；在 THINGS-MEG 上达到 **35.8%**（提升 9.1%）。
*   **几何优势**：实验证实双曲空间比欧几里得空间更适合处理脑-机接口中的模态不平衡问题。
*   **特征融合**：动态插值系数 $t$ 的分布显示，模型在解码时倾向于更多地依赖语义信息（$t < 0.5$），但在处理具有鲜明颜色或形状的图像时会增加感知权重的占比。

#### 7. 优点（亮点）
*   **理论新颖**：首次将双曲几何的测地线插值引入脑-视觉对齐，从几何角度巧妙解决了特征纠缠和信息压缩问题。
*   **通用性强**：实验证明该框架可以即插即用地提升各种视觉编码器和脑编码器的性能。
*   **可解释性**：通过可视化证明了双曲插值确实能让特征向原点收缩，符合神经科学中关于信息瓶颈和抽象表征的预期。

#### 8. 不足与局限
*   **视觉编码器偏差**：该方法在基于 CNN（如ResNet）的视觉模型上表现显著优于 Transformer（如ViT）模型，这可能与 ViT 特征的范数分布特性有关。
*   **增强策略依赖**：性能在一定程度上依赖于手动设计的图像增强手段（如中央凹模糊参数的搜索）。
*   **应用范围**：目前仅在脑图检索任务上验证，尚未扩展到更复杂的图像重建（Image Reconstruction）任务中。

（完）
